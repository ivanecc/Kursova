<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.database2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconka"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            
        </activity>
        <activity android:name=".main"></activity>
        <activity android:name="Dishes"></activity>
        <activity android:name="Rezept_Dish"></activity>
        <activity android:name="First">public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_dishes);</activity>
        <activity android:name="SearchName"><intent-filter>
                

                <action android:name="android.intent.action.MAIN" />public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_dishes);<category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
        <activity android:name="SearchGroup"></activity>
        <activity android:name="SearchGroupDishes"></activity>
    </application>

</manifest>
